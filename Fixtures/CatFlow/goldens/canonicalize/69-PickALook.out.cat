catpipeline 0.8
  pipeline Pick A Look
  gives:   image
params: quality = balanced
presets:
  draft    = steps=4; width=512; height=512
  balanced = steps=9; width=1024; height=1024
  studio   = steps=28; width=1536; height=1536
1. Generate Image   Z-Image Turbo (4-bit); seed=7; preset=(param:quality) "a
                    lighthouse at sunset, watercolor style"
